# AWS Account
aws.region=${config.aws.region}
aws.profile=${config.aws.credentials_profile}
aws.account_id=${config.aws.account_id}

# AWS Cognito
aws.cognito.app_client_id=${config.aws.cognito.app_client_id}
aws.cognito.pool_id=${config.aws.cognito.pool_id}

# AWS IoT
aws.iot.api_version=2015-05-28
aws.iot.mqtt_endpoint=${config.aws.iot.mqtt_endpoint}
